import "./globals.css";
import Header from "../components/layout/Header/Header";

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body>
        <Header />
        {children}
      </body>
    </html>
  );
}
